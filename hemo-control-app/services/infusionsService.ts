import environment from '../environment';
import { InfusionResponse } from '../models';

const defaultHeaders: Record<string, string> = {
    Accept: 'application/json',
    'Content-Type': 'application/json',
}

export const addInfusion = async (accessToken: string): Promise<void> => {
    // To-do
};

export const removeInfusion = async (accessToken: string): Promise<void> => {
    // To-do
};

export const editInfusion = async (accessToken: string): Promise<void> => {
    // To-do
};